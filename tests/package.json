{
  "name": "aboutlastnight-tests",
  "version": "1.0.0",
  "description": "Test suite for About Last Night landing page",
  "scripts": {
    "test:quick": "node run-tests.js --mode=quick",
    "test:comprehensive": "node run-tests.js --mode=comprehensive",
    "test:watch": "node run-tests.js --mode=quick --watch",
    "setup": "npm install && npx playwright install chromium"
  },
  "keywords": ["testing", "playwright", "e2e"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  }
}
