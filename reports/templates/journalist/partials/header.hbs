{{!--
  NovaNews Article Header

  Displays the headline hierarchy (kicker → main → deck),
  byline with author/title/location, and article metadata.

  Context:
    headline: { main, kicker?, deck? }
    byline: { author, title?, location?, date?, guestReporter? }
    metadata: { sessionId, generatedAt, storyDate?, version? }
--}}
<header class="nn-article__header">
  {{!-- Kicker: Small crimson uppercase text above headline --}}
  {{#if headline.kicker}}
  <div class="nn-article__kicker">{{headline.kicker}}</div>
  {{/if}}

  {{!-- Main Headline: Large Zilla Slab --}}
  <h1 class="nn-article__headline">{{headline.main}}</h1>

  {{!-- Deck (subheadline): Optional longer description --}}
  {{#if headline.deck}}
  <p class="nn-article__deck">{{headline.deck}}</p>
  {{/if}}

  {{!-- Article Metadata: Byline + Date + Article ID --}}
  <div class="nn-article__meta">
    {{#if byline.author}}
    <span class="nn-article__byline">
      {{byline.author}}{{#if byline.title}} | {{byline.title}}{{/if}}
    </span>
    {{#if byline.guestReporter}}
    <span class="nn-article__guest-reporter">
      with {{byline.guestReporter}}
    </span>
    {{/if}}
    <span class="nn-article__divider">·</span>
    {{/if}}

    {{#if metadata.storyDate}}
    <time class="nn-article__date" datetime="{{formatDatetime metadata.storyDate}}">
      {{formatDate metadata.storyDate}}
    </time>
    {{else}}
    <time class="nn-article__date" datetime="{{formatDatetime metadata.generatedAt}}">
      {{formatDate metadata.generatedAt}}
    </time>
    {{/if}}

    <span class="nn-article__divider">·</span>

    <span class="nn-article__id">Article {{articleId metadata}}</span>
  </div>
</header>
