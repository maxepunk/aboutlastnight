# Cloudflare Tunnel Configuration Reference
# This is a REFERENCE COPY of the actual config at:
# C:\Users\spide\.cloudflared\config.yml

# DO NOT EDIT THIS FILE - It's just for documentation
# The real config file is managed by cloudflared

# =====================================
# Current Active Configuration
# =====================================

tunnel: f1105ac0-fc4f-4262-827c-e17b4a829fdc
credentials-file: C:\Users\spide\.cloudflared\f1105ac0-fc4f-4262-827c-e17b4a829fdc.json

ingress:
  - hostname: console.aboutlastnightgame.com
    service: http://localhost:3000
  - service: http_status:404

# =====================================
# What each line means:
# =====================================
# tunnel: The unique ID from cloudflared tunnel create
# credentials-file: Path to the JSON credentials file
# hostname: Your public URL (console.aboutlastnightgame.com)
# service: Where to route traffic (http://localhost:3000 = your server)
# Last line: Catch-all rule (returns 404 for any other hostname)

# =====================================
# How to view the actual config:
# =====================================
# Windows:
#   type C:\Users\spide\.cloudflared\config.yml
#
# Mac/Linux:
#   cat ~/.cloudflared/config.yml

# =====================================
# How to test if tunnel is working:
# =====================================
# 1. Check tunnel exists:
#    cloudflared tunnel list
#
# 2. Get tunnel details:
#    cloudflared tunnel info aln-console
#
# 3. Run tunnel:
#    cloudflared tunnel run aln-console
#
# 4. Visit:
#    https://console.aboutlastnightgame.com

# =====================================
# Setting up on a NEW computer
# =====================================
# If you need to set this up on a different machine:
#
# 1. Install cloudflared
# 2. Authenticate: cloudflared tunnel login
# 3. Create tunnel: cloudflared tunnel create aln-console
# 4. Route DNS: cloudflared tunnel route dns aln-console console.aboutlastnightgame.com
# 5. Create config file at C:\Users\YourUsername\.cloudflared\config.yml
#    (copy the structure above, but use YOUR tunnel ID)
# 6. Run: cloudflared tunnel run aln-console
#
# See SETUP_CHECKLIST_CLOUDFLARE.md for detailed instructions
